# 文章发布平台 - 前端

一个专业的多平台文章发布系统的前端部分，使用 Vue 3、Element Plus、TypeScript 和 Pinia 开发。

## 📋 项目特性

✅ Vue 3 Composition API + TypeScript  
✅ Element Plus UI 组件库  
✅ Pinia 状态管理  
✅ Markdown 实时编辑和预览  
✅ 4 种主题切换  
✅ 深色/浅色模式  
✅ 16 个平台配置支持  
✅ 响应式设计  
✅ JWT 认证  

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0 或 pnpm >= 7.0.0

### 安装依赖

\`\`\`bash
cd frontend

# 使用 npm
npm install

# 或使用 pnpm（推荐）
pnpm install

# 或使用 yarn
yarn install
\`\`\`

### 开发模式

\`\`\`bash
npm run dev
# 或
pnpm dev
\`\`\`

应用将在 http://localhost:5173 启动。自动代理 API 请求到 http://localhost:8080

### 构建生产版本

\`\`\`bash
npm run build
# 或
pnpm build
\`\`\`

生成的文件在 `dist` 目录中。

### 类型检查

\`\`\`bash
npm run type-check
# 或
pnpm type-check
\`\`\`

## 📁 项目结构

\`\`\`
frontend/
├── src/
│   ├── components/          # 可复用组件
│   ├── layouts/            # 布局组件
│   ├── pages/              # 页面组件
│   ├── router/             # 路由配置
│   ├── stores/             # Pinia 状态仓库
│   │   ├── authStore.ts    # 认证状态
│   │   ├── articleStore.ts # 文章状态
│   │   └── themeStore.ts   # 主题状态
│   ├── types/              # TypeScript 类型定义
│   ├── utils/              # 工具函数
│   │   └── api.ts          # Axios 实例配置
│   ├── App.vue             # 根组件
│   ├── main.ts             # 应用入口
│   └── index.css           # 全局样式
├── public/                 # 静态资源
├── index.html             # HTML 入口
├── vite.config.ts         # Vite 配置
├── tsconfig.json          # TypeScript 配置
├── package.json           # 项目依赖
└── README.md              # 本文件
\`\`\`

## 🔐 认证流程

1. 用户在登录页面输入用户名和密码
2. 前端调用 `/api/auth/login` 接口
3. 后端验证用户身份并返回 JWT Token
4. 前端将 Token 存储在 localStorage
5. 后续请求自动在 Authorization 头中包含 Token
6. 路由守卫检查用户认证状态

## 📝 页面说明

### 登录页面 (`/login`)
- 用户名和密码输入
- 错误提示
- 跳转到注册页面

### 注册页面 (`/register`)
- 用户名、邮箱、密码输入
- 密码确认
- 注册成功后跳转到登录页面

### 仪表板 (`/`)
- 文章统计信息
- 快速操作按钮
- 文章概览

### 编辑页面 (`/editor`)
- Markdown 编辑器
- 实时预览
- 主题选择
- 保存为草稿

### 文章管理 (`/articles`)
- 文章列表
- 搜索和筛选
- 编辑、发布、删除操作
- 状态显示

### 平台配置 (`/platforms`)
- 所有 16 个平台的卡片
- 平台连接/断开
- 账户信息管理

### 账户设置 (`/settings`)
- 个人信息编辑
- 主题选择
- 深色模式开关

## 🎨 主题系统

系统内置 4 种主题，可在设置页面切换：

1. **默认主题** - 蓝色调
2. **深色专业** - 深色背景
3. **学术主题** - 蓝色学术风格
4. **炫彩渐变** - 彩色温暖风格

## 🔌 API 集成

前端通过 Axios 与后端 API 通信，所有 API 请求自动添加：

- Authorization 头（JWT Token）
- Content-Type: application/json
- 错误拦截和 Toast 提示

### 主要 API 端点

\`\`\`
POST   /api/auth/login          # 用户登录
POST   /api/auth/register       # 用户注册
GET    /api/auth/user           # 获取当前用户
GET    /api/articles            # 获取文章列表
GET    /api/articles/:id        # 获取单篇文章
POST   /api/articles            # 创建文章
PUT    /api/articles/:id        # 更新文章
DELETE /api/articles/:id        # 删除文章
POST   /api/publish             # 发布到多个平台
\`\`\`

## 🛠️ 开发指南

### 添加新页面

1. 在 `src/pages/` 创建 `.vue` 文件
2. 在 `src/router/index.ts` 中添加路由
3. 在导航菜单中添加链接

### 添加新组件

1. 在 `src/components/` 创建 `.vue` 文件
2. 使用 TypeScript 和 Composition API
3. 导入到需要的页面

### 添加状态

1. 在 `src/stores/` 创建新的 Store
2. 使用 Pinia 的 `defineStore`
3. 在组件中使用 `useStore()`

### 样式指南

- 使用 Tailwind CSS 类名
- 使用 CSS 变量定义颜色
- 遵循 BEM 命名规范
- 支持深色模式

## 📦 依赖说明

| 依赖 | 版本 | 说明 |
|------|------|------|
| vue | ^3.4.21 | 核心框架 |
| element-plus | ^2.7.6 | UI 组件库 |
| pinia | ^2.1.7 | 状态管理 |
| axios | ^1.7.2 | HTTP 客户端 |
| marked | ^12.0.0 | Markdown 解析 |
| vite | ^5.0.8 | 构建工具 |
| typescript | ^5.3.3 | 类型检查 |
| tailwindcss | ^3.4.1 | CSS 框架 |

## 🌍 环境变量

创建 `.env` 文件（可选）：

\`\`\`env
VITE_API_BASE_URL=http://localhost:8080/api
VITE_APP_TITLE=文章发布平台
\`\`\`

在 `vite.config.ts` 中已配置 API 代理，开发环境请求自动转发到后端。

## 📱 响应式支持

应用支持以下屏幕尺寸：

- 📱 手机：< 768px
- 📱 平板：768px - 1024px
- 💻 桌面：> 1024px

## 🐛 常见问题

### Q: 前后端代理不工作？
A: 确保后端运行在 http://localhost:8080，检查 `vite.config.ts` 中的代理配置。

### Q: 样式加载异常？
A: 清除浏览器缓存，重新运行 `npm run dev`。

### Q: 编辑器不显示？
A: 检查 marked 库是否正确安装，运行 `npm install marked --save`。

## 🚀 部署

### Vercel 部署

\`\`\`bash
npm run build
# 然后连接到 Vercel，选择 dist 文件夹
\`\`\`

### 自托管部署

\`\`\`bash
npm run build
# 将 dist 文件夹内容部署到 Web 服务器
\`\`\`

### Nginx 配置

\`\`\`nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://localhost:8080;
    }
}
\`\`\`

## 📄 许可证

MIT

## 👨‍💻 技术支持

如有问题，请提交 Issue 或 Pull Request。

---

**开发者**: v0 AI Assistant  
**最后更新**: 2025年1月  
**版本**: 1.0.0
